# Task

Ensure `greet(name: str) -> str` in `src/greeting.py` satisfies:

- Trim surrounding whitespace
- Empty/whitespace raises ValueError
- Return "Hello, <name>!"
- Preserve existing behavior
- Run tests
